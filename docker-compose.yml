services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-https://spa-entreprise-api.onrender.com/api/v1}
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: production
